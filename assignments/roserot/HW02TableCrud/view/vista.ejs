<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= titulo %></title>
   
</head>
<body>

    <div class="container">
        <br>
    <div class="jumbotron">
        <h1><%= titulo %></h1>

        <form action="/add" method="POST">
            <legend>Add medicine</legend>


            <div class="form-group">
                <input type="text" placeholder="Id" id="indice" class="form-control" name="indice">
            </div>
            <div class="form-group">
                <textarea placeholder="Medicine" class="form-control" name="nombreMedicina" rows="1" cols="80"></textarea>
            </div>
            <div class="form-group">
                <textarea placeholder="Value $" class="form-control" name="precio" rows="1" cols="80"></textarea>
            </div>
            <div class="form-group">
                <textarea placeholder="Pharmacy" class="form-control" name="farmaceutica" rows="1" cols="80"></textarea>
            </div>
            <div class="form-group">
                <textarea placeholder="Description" class="form-control" name="descripcionMedicina" rows="1" cols="80"></textarea>
            </div>


            <button class="btn btn-primary">✔ Add</button>
        </form>
        </div>

        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>indice</th>
                    <th>medicina</th>
                    <th>precio</th>
                    <th>farmaceutica</th>
                    <th>descripcion</th>
                </tr>
            </thead>
            <tbody>
                <% for(var i=0; i < medicines.length; i++){ %>
                <tr>
                    <td><center><strong><%= medicines[i].indice %></strong></center></td>
                    <td><center><strong><%= medicines[i].nombreMedicina %></strong></center></td>
                    <td><center><strong><%= medicines[i].precio %></strong></center></td>
                    <td><center><strong><%= medicines[i].farmaceutica %></strong></center></td>
                    <td><center><strong><%= medicines[i].descripcionMedicina %></strong></center></td>
                    <td><center></center></td>
                    <td>
                        <a id="eliminar" class="btn btn-danger" href="/delete/<%= medicines[i]._id %>">💣Erase</a>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>
    
</body>
</html>